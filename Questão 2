def soma_imposto():
    ct = (custo * taxa_imposto / 100)
    print('o preço do produto com os impostos será de R${:.2f}.'.format(custo + ct))

custo = float(input('Custo:'))
taxa_imposto = float(input('Taxa de imposto em %:'))

soma_imposto()
